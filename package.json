{
  "name": "seo-content-outline-toolkit",
  "version": "1.0.0",
  "description": "SEO keyword volume, SERP heading research, and sheet-ready content outline toolkit.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "scripts": {
    "volume": "node scripts/keyword-volume.mjs",
    "headings": "node scripts/serp-headings.mjs",
    "outline": "node scripts/generate-outline-tsv.mjs",
    "check": "node scripts/keyword-volume.mjs --help && node scripts/serp-headings.mjs --help && node scripts/generate-outline-tsv.mjs --help"
  },
  "engines": {
    "node": ">=20"
  }
}

